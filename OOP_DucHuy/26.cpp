

template <class T>
void Func(T var_x)
{
    cout << var_x;
}

// A. A function taking a single generic parameter and returning a specific non-void type
// B. A function taking a single generic parameter and returning a generic type
// C. All of these above
// D. A function taking a single generic parameter and returning nothing
// E. A function taking single int parameter and returning a generic type

// Đáp án: D. A function taking a single generic parameter and returning nothing